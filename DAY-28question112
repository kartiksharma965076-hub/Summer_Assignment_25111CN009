
#include <stdio.h>
#include <string.h>

struct Contact {
    char name[40];
    char phone[20];
};

int main() {
    struct Contact directory[10];
    int size = 0, choice, i, found;
    char lookup[40];

    while (1) {
        printf("\n*** Personal Contact Index Directory ***\n");
        printf("1. Add New Contact\n");
        printf("2. Show Registered Directory\n");
        printf("3. Lookup Contact By Name\n");
        printf("4. Quit Application\n");
        printf("Enter system prompt index: ");
        scanf("%d", &choice);

        switch (choice) {
            case 1:
                if (size  Name: %s, Mobile Phone: %s\n", directory[i].name, directory[i].phone);
                        found = 1;
                        break;
                    }
                }
                if (!found) printf("No registry matched that specific query criteria context.\n");
                break;
            case 4:
                return 0;
            default:
                printf("Input menu target selector does not match listed indices.\n");
        }
    }
    return 0;
}
